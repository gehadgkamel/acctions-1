name: Generate ASCII Artwork

on: push

jobs:
  ascii_job:
    runs-on: ubuntu-latest
    steps:
    - name: checkout repo
      uses: actions/checkout@v4

    - name: list repo files
      run: ls -ltra

    - name: executing sell script
      run: |
        chmod +x ascii-script.sh
        ./ascii-script.sh

